function result = Testing(n, d, t, roads)   

    diff = zeros(251, 4);
    
%     Differences for two trapizoidal bumps - lateral disturbance
% We want this to be as small as possible
    diff(: , 1) = roads(:,1) - lsim(n,d,roads(:,1),t); 
    diff(: , 4) = roads(:,4) - lsim(n,d,roads(:,4),t); 
    
%     Differences for verticle disturbances bumps
   flat_road = zeros(251, 1);
    diff(: , 2) = roads(:,2) - lsim(n,d,roads(:,2),t); 
    diff(: , 4) = roads(:,1) - lsim(n,d,roads(:,1),t); 


end